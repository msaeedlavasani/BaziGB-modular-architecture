# 🎨 BaziGB — استاندارد طراحی Elite (DESIGN_SYSTEM.md)

این سند قرارداد بصری پلتفرم BaziGB است. هر کامپوننت UI باید از این استاندارد پیروی کند.

## ۱. پالت رنگ — Honey Bronze

| توکن | مقدار | کاربرد |
| --- | --- | --- |
| `primary.main` | `#EEAC2F` | برنز عسلی — دکمههای اصلی، برند، عناصر فعال |
| `primary.light` | `#FFD27A` | درخشش/هایلایت |
| `primary.dark` | `#B97F12` | hover/فشردهشده |
| `secondary.main` | `#061A2D` | سرمهای تیره — هدر، فوتر، پسزمینه عمیق |
| `secondary.light` | `#1B3550` | سطوح بالاتر |
| `bgDeep` | `#0B1622` | پسزمینه صفحه |
| `bgPaper` | `#132236` | کارت/کاغذ |
| `border` | `#2A3F57` | خطوط جداکننده |
| `textMain` | `#F5EFE4` | متن اصلی |
| `textMuted` | `#A9B7C6` | متن فرعی |

**قانون طلایی:** هیچ رنگ پیشفرض MUI (Indigo/Slate/Rose) در هیچجای UI استفاده نمیشود. همه رنگها از توکنهای بالا.

## ۲. RTL و چیدمان (آینه)

- زبان: فارسی، جهت: `rtl`، فونت: **Vazirmatn**
- **هدر:** برند BaziGB در **دورترین راست**، آیکونهای ابزار (پروفایل، صدا) در **دورترین چپ**
- **فوتر:** ترتیب `[نماد (چپ)] --- [لینکها (وسط)] --- [BaziGB (راست)]`
- همه فرزندان فلکس `minWidth: 0` دارند (جلوگیری از سرریز متن)

## ۳. حس فیزیکی قطعات (Tactility)

قطعات بازی باید مانند اشیاء واقعی دیده شوند:

- **خانههای برد:** سایه داخلی (`boxShadow: inset`)
- **مهرهها/مهرههای نرد:** گرادیان شعاعی + چند سایه (`radial-gradient` + `inset`)
- **عناصر تعاملی:** درخشش ملایم (glow) هنگام hover با `rgba(238,172,47, 0.3~0.6)`
- **چوب بردها:** گرادیان قهوهای چوب طبیعی (`#4A3420 → #241708`)

## ۴. حالتهای کامپوننت

- **Loading:** هر بخش وابسته به داده، اسکلت ساختاری دارد (`LoadingSkeleton`)
- **Empty:** حالت خالی با آیکون سفارشی BaziGB + دکمه CTA Honey Bronze (`EmptyState`)
- **بردها:** خانههای قابل انتخاب هنگام hover درخشش دارند؛ خانه هدف با نقطه برنزی نشان داده میشود

## ۵. واکنشگرایی

- **موبایل ۳۶۰px:** صفر اسکرول افقی
- هدر شناور و دکمهها به safe-area گوشی احترام میگذارند

## ۶. بردها

| برد | مشخصه |
| --- | --- |
| دوز | خانههای سرمهای با سایه داخلی، مهره ✕ برنزی درخشان، ◯ آبی روشن |
| نرد | چوب طبیعی، مثلثهای متناوب، مهره برنز/سرمه با گرادیان شعاعی، زندان و خارج در کنارهها |
| شطرنج | خانههای روشن چوبی/تیره، مهرههای عاج و آبنوس، هایلایت انتخاب برنزی |
| وگاس | مسیر آبی با بخشبندی، مهرههای فیزیکی، پرچم پایان |
